apiVersion: v1
kind: Secret
metadata:
  name: railpush-r2-backup
  namespace: railpush
type: Opaque
stringData:
  accessKeyId: "CHANGE_ME"
  secretAccessKey: "CHANGE_ME"
  # Cloudflare R2 uses region "auto".
  region: "auto"
