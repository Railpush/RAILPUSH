apiVersion: v1
kind: Secret
metadata:
  name: cloudflare-api-token-secret
  namespace: cert-manager
type: Opaque
stringData:
  # Cloudflare API Token with "Zone.DNS:Edit" for railpush.com
  api-token: "CHANGE_ME"
